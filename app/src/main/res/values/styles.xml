<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>

        <!-- text colors -->
        <item name="android:textColorPrimary">@color/_textColorPrimary</item>
        <item name="android:textColorSecondary">@color/_colorBackgroundSecondary</item>

        <!-- background colors -->
        <item name="android:colorBackground">@color/_colorBackgroundPrimary</item>
        <item name="colorBackgroundSecondary">@color/_colorBackgroundSecondary</item>

        <!-- button related business -->
        <item name="primaryButtonBackground">@color/_primaryButtonBackground</item>
        <item name="primaryButtonTextColor">@color/_primaryButtonTextColor</item>
    </style>

    <style name="TextAppearance">
        <item name="android:textColor">?android:textColorPrimary</item>
    </style>

    <style name="PrimaryPillButton">
        <item name="android:background">@drawable/primary_btn_background_small</item>
        <item name="android:textColor">?primaryButtonTextColor</item>
    </style>

    <style name="PrimaryPillButtonSmall" parent="PrimaryPillButton">
        <item name="android:textSize">20dp</item>
        <item name="android:textAllCaps">false</item>
    </style>

</resources>